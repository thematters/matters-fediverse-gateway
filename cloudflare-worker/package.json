{
  "name": "matters-fediverse-gateway-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker edge demo for Matters Fediverse Gateway.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "dry-run": "wrangler deploy --dry-run",
    "test": "node --test",
    "check": "wrangler deploy --dry-run",
    "check:follow-readiness": "node scripts/check-follow-readiness.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.14.0"
  }
}
